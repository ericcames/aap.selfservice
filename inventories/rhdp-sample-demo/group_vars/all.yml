---
# Self-Service Portal Bootstrap Inventory
# Copy this directory to inventories/rhdp-<customer>-<demo>/
# All sensitive values are resolved at runtime from environment variables
# or files under ~/.ansible/ — never hardcode them here.

# AAP Controller (env-resolved at runtime)
aap_hostname: "{{ lookup('ansible.builtin.env', 'CONTROLLER_HOST') }}"
aap_username: "{{ lookup('ansible.builtin.env', 'CONTROLLER_USERNAME') | default('admin', true) }}"
aap_password: "{{ lookup('ansible.builtin.env', 'CONTROLLER_PASSWORD') }}"
aap_validate_certs: false

# OpenShift (env-resolved at runtime)
ocp_host: "{{ lookup('ansible.builtin.env', 'OCP_HOST') }}"
ocp_token: "{{ lookup('ansible.builtin.env', 'OCP_TOKEN') }}"

# Automation Hub (token resolved from ~/.ansible/ansible.cfg)
hub_token: "{{ lookup('ini', 'token section=galaxy_server.rh_certified file=~/.ansible/ansible.cfg') }}"
hub_auth_url: "https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token"

# Portal
portal_namespace: aap-portal
