---
profile: basic

# Don't attempt to install collections from Galaxy/Hub in CI
offline: true

exclude_paths:
  - inventories/rhdp-sample-demo/

# Warn rather than fail on rules that require live collection resolution
warn_list:
  - fqcn
  - no-changed-when
